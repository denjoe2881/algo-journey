import { defineExercise } from '../../_loader';
export default defineExercise({ id: 'simple-counter', version: 1, title: 'Simple Counter', summary: 'Implement a counter class with increment and getValue.', topic: 'classes', difficulty: 'easy', tags: ['constructor', 'state'], estimatedMinutes: 15, order: 1, mode: 'class_implementation', learningGoals: ['Create a class with state', 'Use constructor and methods'], statement: 'Implement a `Counter` class with: a constructor that initializes the count to 0, an `increment()` method that adds 1 to the count, and a `getValue()` method that returns the current count.', constraints: ['increment() is called at most 10^6 times.'], examples: [{ input: 'Counter c = new Counter(); c.increment(); c.increment(); c.getValue();', output: '2' }],
  starter: { file: 'Counter.java', code: `class Counter {\n    Counter() {\n        // Write your code here\n    }\n\n    void increment() {\n        // Write your code here\n    }\n\n    int getValue() {\n        // Write your code here\n        return 0;\n    }\n}` },
  requiredStructure: { className: 'Counter', requiredMethods: ['Counter()', 'void increment()', 'int getValue()'] }, evaluation: { comparator: 'exact_json' } });
